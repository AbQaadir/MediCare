1 ->
Add this table to the database

CREATE TABLE shipping_info (
    id INT AUTO_INCREMENT PRIMARY KEY,
    ship_name VARCHAR(255) NOT NULL,
    ship_number VARCHAR(20) NOT NULL,
    ship_address VARCHAR(255) NOT NULL,
    ship_city VARCHAR(100) NOT NULL,
    ship_zip VARCHAR(20) NOT NULL,
    ship_country VARCHAR(100) NOT NULL,
    ship_email VARCHAR(255) NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);


2 ->
join the procced to checkout button with shipping.php


3 -> CURD operation

    for product delete operation we must want to the admin email
    so add a hidden input felid in the file name "upload.view.php"

    code:
    <input type="hidden" name="session_email" value="<?php $_session["email"]?>"

    Then
    add a column name called "email" in the products table

    code:
    ALTER TABLE products
    ADD COLUMN email VARCHAR(255);

    Then
    Fully delete all the products table data and add new data to it. 

    "Ect........."



